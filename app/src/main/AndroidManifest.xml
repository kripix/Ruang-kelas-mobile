<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ruang_kelas_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ruang_kelas_icon"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.RuangKelas"
        tools:targetApi="31">

        <activity
            android:name=".ui.kelas.pertemuan.detail.LatihanMultimediaActivity"
            android:exported="false" />
        <activity
            android:name=".ui.kelas.pertemuan.detail.LatihanSharedPreferenceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.kelas.pertemuan.detail.DetailPertemuanActivity"
            android:exported="false" />
        <activity
            android:name=".ui.kelas.pertemuan.RuangActivity"
            android:exported="false" />
        <activity
            android:name=".ui.kelas.navbar.notifikasi.NotificationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.kelas.tambah.TambahKelasActivity"
            android:exported="false" />
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>